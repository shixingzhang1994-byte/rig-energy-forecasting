# 项目工作规范

本文件约束后续在 `/home/zsx/桌面/QZ` 内的所有工作。若任务另有明确要求，以用户当次指令为准；不得自行扩大范围。

## 工作原则

1. 默认简洁输出，先说结论，不重复已知背景。
2. 每次只处理一个明确问题。任务开始前必须写明“任务目标”和可检查的“验收条件”。
3. 完成当前任务并达到验收条件后立即停止，不自行进入下一阶段。
4. 禁止为了“完整性”“创新性”或展示能力，无目的增加模型、模块、实验、功能、指标、数据集或文档。
5. 新增任何工作前，必须说明它具体解决哪个项目问题；无法对应明确问题时不得开展。
6. 优先级始终为：正确性、数据可信度、实验可信度、可复现性，其次才是功能数量。
7. 不随意改变已经冻结的数据、参数、种子、配置、统计口径、实验协议、证据路径和研究边界。确需变更时，先说明原因、影响和新旧边界，取得用户明确同意，并保留原证据。
8. 明确区分“已实现”“已测试”“已运行实验”“已通过实验验证”“已获得真实场景证据”；不得用前一层替代后一层。
9. 不在聊天中粘贴大段代码或完整日志；只给必要摘要，并用文件路径指向证据。
10. 修改前检查现有文件和 Git 状态，保留用户已有改动，不覆盖、不重命名、不清理冻结产物。

## 任务边界

- 未经用户明确要求，不继续新增开发、实验或功能扩展。
- 诊断或审计任务默认只读；除任务明确指定的状态/报告文件外，不修改项目。
- 不以补齐数字、追求正结果或绕过门槛为理由补种子、替换样本、改变阈值或重定义分析总体。
- 发现证据不足、结果为负或协议不完整时，如实记录，不用新工作掩盖。
- 下一步建议最多两项，由用户决定是否执行；不得自动执行。

## 状态与证据

- `PROJECT_STATUS.md` 是精简的当前项目状态入口，不是历史流水账。
- 历史记录、旧版本和审计材料仅用于追溯；当前结论必须指向冻结配置或机器可读结果。
- 若文档与冻结产物冲突，先报告冲突，不自行选择有利版本或改写冻结结果。
- 任何结论必须同时注明证据类型和适用边界，尤其区分仿真、公开真实数据观测、目标钻机现场/HIL 与部署证据。

## Usage-limit safety

考虑到 Codex 存在使用额度限制，任何较长任务都必须设计为可中断、可恢复的独立阶段。

- 不允许把大量代码修改、长时间实验和结果分析串成一个不可中断的大任务。
- 每完成一个可验证阶段，立即保存当前状态并更新 `PROJECT_STATUS.md`。
- 实验必须保留配置、随机种子、输出目录和对应代码版本，保证额度耗尽后可以恢复。
- 在启动预计耗时较长或涉及大量模型调用的任务前，先说明本阶段目标和完成条件。
- 如果当前任务可能跨越额度限制，必须拆分为多个独立 checkpoint。
- 不得假定额度恢复后可以根据聊天上下文无损继续。
- 如果任务被额度限制中断，下次恢复后首先读取 `PROJECT_STATUS.md` 和已有实验结果，确认中断位置，不得直接继续执行。

## 中断恢复规则

如果任务因额度限制、会话中断、程序异常或其他原因未正常结束，下次恢复时禁止直接继续。

必须先执行“中断恢复检查”：

- 读取 `PROJECT_STATUS.md`；
- 检查 Git 状态、最近修改文件、实验输出和日志；
- 判断上次任务属于：已完成并验证、已完成未验证、部分完成、实验未完成、状态不确定；
- 区分已经验证的结果和半成品修改；
- 找到当前最安全的恢复点；
- 只提出一个最小恢复任务；
- 在用户确认前，不修改代码、不运行实验、不创建新版本。

恢复判断必须以仓库文件、实验结果和 `PROJECT_STATUS.md` 为准，不得仅凭聊天上下文推测。

恢复检查完成后，只简要汇报：

- 中断位置
- 已完成
- 未完成
- 风险
- 推荐恢复点
- 下一步一个最小任务

汇报后立即停止，等待用户决定是否继续。

## 完成汇报格式

每次任务完成后只汇报以下内容，保持简洁：

1. 结论
2. 完成内容
3. 关键结果
4. 风险
5. 修改文件
6. 是否达到验收条件
7. 下一步建议（最多两项，不自动执行）
